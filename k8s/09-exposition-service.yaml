apiVersion: v1
kind: Service
metadata:
  name: pgrest-service
  namespace: desafio-k8s
spec:
  selector:
    app: pgrest
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: LoadBalancer
